import { Dialog } from "@mui/material";
import Button from "@mui/material";


const UserInfo = ({current_balance, full_name, department, id}) => {
    return <div className="user_info">
        <Button>WTF is going on</Button>
    </div>
}



export default UserInfo;